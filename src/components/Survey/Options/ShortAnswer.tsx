'use client'

const ShortAnswer = () => {

    return (
        <label>
            <input className={`border`} type="text" maxLength={50} />
            세
        </label>
    );
}

export default ShortAnswer;